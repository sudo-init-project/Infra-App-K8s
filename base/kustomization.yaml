apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: proyecto-cloud

resources:
  - namespace.yaml
  # Remover secrets.yaml problemático
  # - secrets.yaml
  
  # Usar Sealed Secrets en su lugar (se crearán con el script)
  - mysql-sealed-secret.yaml
  - app-sealed-secret.yaml
  
  - db/configmap.yaml
  - db/statefulset.yaml
  - db/service.yaml
  - back/deployment.yaml
  - back/service.yaml
  - front/deployment.yaml
  - front/service.yaml

images:
  - name: facundo676/backend-shop
    newTag: latest
  - name: facundo676/frontend-shop
    newTag: latest
