replicaCount: 2

image:
  repository: facundo676/backend-shop
  tag: "20251221-232309-71c9e37"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

env:
  SPRING_PROFILES_ACTIVE: "demo"
  SERVER_PORT: "8080"